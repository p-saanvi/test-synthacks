# test-synthacks

this is a test to teach saanvi how to use github

## What this does

This repo contains a simple "Hello, World!" example:

- `hello.py` — a Python script that prints `Hello, World!` to the console.
- `hello_world.svg` — a terminal-style graphic showing the script and its output.

Run it with:

```bash
python3 hello.py
```
